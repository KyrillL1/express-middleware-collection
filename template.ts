import { Application } from "express-serve-static-core";

/**
 * Replace applyYourMiddleware with a new name
 * Don't forget to export it
 * @param app 
 */
const applyYourMiddleware = (app: Application) => {
    // do something with the app
}

export default name_of_your_middleware;